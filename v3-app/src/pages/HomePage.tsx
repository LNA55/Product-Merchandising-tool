/** User home page of the app (not a marketing site). Intentionally empty for now. */
export function HomePage() {
  return (
    <div className="flex min-h-[60vh] items-center justify-center px-8 py-8">
      <p className="text-[13px] text-stone-300">Home — content to be defined.</p>
    </div>
  )
}
